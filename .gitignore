# Secrets — never commit
.env
.env.local
.calendly-users.json

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Local DB (contains run history with real response text from your Calendly account)
mcp_testing.db
mcp_testing.db-journal

# Account-specific fixture state captured by setup script — not useful
# across different Calendly accounts.
backups/coffee_chat_baseline_rules.json
backups/fixture_state_*.json
data.db

# OS / editor
.DS_Store
.vscode/
.idea/
